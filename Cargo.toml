[package]
name = "cntryl-midge"
version = "0.1.0"
edition = "2021"
rust-version = "1.93"
description = "A high-performance embedded key-value database with LSM-tree storage"
license = "Apache-2.0"
repository = "https://github.com/cntryl/midge"
readme = "README.md"
keywords = ["database", "kv", "lsm", "embedded", "storage"]
categories = ["database-implementations"]

[lib]
name = "cntryl_midge"
path = "src/lib.rs"

# =============================================================================
# Dependencies
# =============================================================================

[dependencies]
# Concurrency & Synchronization
arc-swap = "1.7"
crossbeam = "0.8"
crossbeam-channel = "0.5"
crossbeam-epoch = "0.9"
dashmap = "6.1"
# Note: fail is used for deterministic crash testing in test suites.
# In production builds, this has minimal overhead. For security-critical environments,
# consider building without this dependency if failpoint testing is not needed.
fail = { version = "0.5", features = ["failpoints"] }
parking_lot = "0.12"

# Utilities used by stress workloads
rand = "0.10"
hdrhistogram = "7.3"

# Compression
lz4_flex = "0.13"
zstd = "0.13"

# Data Types & Serialization
bincode = "1.3"
bytes = { version = "1.10", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.18", features = ["v4"] }

# Error Handling
anyhow = "1.0"
thiserror = "2.0"

# Hashing
crc32c = "0.6"
crc32fast = "1.5"
xxhash-rust = { version = "0.8", features = ["xxh3"] }

# Logging & Tracing
chrono = { version = "0.4", features = ["serde"] }
log = "0.4"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

# OpenTelemetry (Observability)
opentelemetry = { version = "0.20", optional = true, features = ["rt-tokio"] }
opentelemetry-otlp = { version = "0.13", optional = true, features = ["tokio"] }
tracing-opentelemetry = { version = "0.21", optional = true }

# System Utilities
byteorder = "1.5"
hex = "0.4"
hostname = "0.4"
num_cpus = "1.16"
once_cell = "1.21"
regex = "1.11"
sysinfo = "0.38"
tempfile = "3.23"

# Cloud Backend Support (optional)
base64 = { version = "0.22", optional = true }
futures = { version = "0.3", optional = true }
hmac = { version = "0.12", optional = true }
md5 = { version = "0.8", optional = true }
native-tls = { version = "0.2.18", optional = true }
percent-encoding = { version = "2.3", optional = true }
reqwest = { version = "0.13", default-features = false, features = ["json", "native-tls", "blocking"], optional = true }
sha2 = { version = "0.10", optional = true }
tokio = { version = "1.40", features = ["rt-multi-thread", "macros"], optional = true }
ureq = { version = "3", features = ["rustls", "json"], optional = true }
url = { version = "2.5", optional = true }
urlencoding = { version = "2.1", optional = true }

# Platform-Specific Dependencies

[target.'cfg(unix)'.dependencies]
libc = "0.2"
# Platform-Specific: Linux (io_uring - optional)
io-uring = { version = "0.7", optional = true }

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["fileapi", "winbase"] }

[dev-dependencies]
clap = { version = "4.5", features = ["derive"] }
criterion = "0.8"
paste = "1.0"
proptest = "1.8"
cntryl-stress = "0.2.0"

# =============================================================================
# Features
# =============================================================================

[features]
default = ["cloud-common"]
telemetry = ["dep:opentelemetry", "dep:tracing-opentelemetry"]
telemetry-otlp = ["telemetry", "dep:opentelemetry-otlp"]
uring = ["dep:io-uring"]

# Cloud Storage Backends
cloud-common = [
    "dep:ureq",
    "dep:hmac",
    "dep:sha2",
    "dep:base64",
    "dep:url",
    "dep:tokio",
    "dep:reqwest",
    "dep:native-tls",
    "dep:percent-encoding",
    "dep:urlencoding",
]
cloud-aws = ["cloud-common", "dep:md5"]

# Feature gate to expose `testkit` types from crate root when enabled.
# Default is *not* to expose test-only helper types to downstream consumers.
testkit = []
cloud-azure = ["cloud-common"]
cloud-gcp = ["cloud-common"]
cloud-oci = ["cloud-common"]
cloud-all = ["cloud-aws", "cloud-azure", "cloud-gcp", "cloud-oci"]
cloud_integration = ["cloud-all"]


# =============================================================================
# Benchmarks — Tier 1: Hot Path (<1s)
# =============================================================================

[[bench]]
name = "tier1_hotpath_api"
path = "benches/tier1_hotpath_api.rs"
harness = false

[[bench]]
name = "tier1_hotpath_bloom"
path = "benches/tier1_hotpath_bloom.rs"
harness = false

[[bench]]
name = "tier1_hotpath_block_cache"
path = "benches/tier1_hotpath_block_cache.rs"
harness = false

[[bench]]
name = "tier1_hotpath_iterator"
path = "benches/tier1_hotpath_iterator.rs"
harness = false

[[bench]]
name = "tier1_hotpath_memtable"
path = "benches/tier1_hotpath_memtable.rs"
harness = false

[[bench]]
name = "tier1_hotpath_sst"
path = "benches/tier1_hotpath_sst.rs"
harness = false

[[bench]]
name = "tier1_hotpath_sparse_index"
path = "benches/tier1_hotpath_sparse_index.rs"
harness = false

[[bench]]
name = "tier1_hotpath_trie"
path = "benches/tier1_hotpath_trie.rs"
harness = false

[[bench]]
name = "tier1_hotpath_wal"
path = "benches/tier1_hotpath_wal.rs"
harness = false

[[bench]]
name = "tier1_hotpath_tlv_encoding"
path = "benches/tier1_hotpath_tlv_encoding.rs"
harness = false

[[bench]]
name = "tier1_hotpath_singleflight"
path = "benches/tier1_hotpath_singleflight.rs"
harness = false

[[bench]]
name = "tier1_hotpath_compression"
path = "benches/tier1_hotpath_compression.rs"
harness = false

[[bench]]
name = "tier1_hotpath_event_loop"
path = "benches/tier1_hotpath_event_loop.rs"
harness = false

[[bench]]
name = "tier2_subsystem_event_loop"
path = "benches/tier2_subsystem_event_loop.rs"
harness = false

# =============================================================================
# Benchmarks — Tier 2: Subsystem (<8s)
# =============================================================================

[[bench]]
name = "tier2_subsystem_block_cache"
path = "benches/tier2_subsystem_block_cache.rs"
harness = false

[[bench]]
name = "tier2_subsystem_bloom_build"
path = "benches/tier2_subsystem_bloom_build.rs"
harness = false

[[bench]]
name = "tier2_subsystem_sst_point_read_bloom"
path = "benches/tier2_subsystem_sst_point_read_bloom.rs"
harness = false

[[bench]]
name = "tier2_subsystem_range_scan_cache"
path = "benches/tier2_subsystem_range_scan_cache.rs"
harness = false

[[bench]]
name = "tier2_subsystem_iterator_multi_sst"
path = "benches/tier2_subsystem_iterator_multi_sst.rs"
harness = false

[[bench]]
name = "tier2_subsystem_memtable_rotate"
path = "benches/tier2_subsystem_memtable_rotate.rs"
harness = false

[[bench]]
name = "tier2_subsystem_read_amplification"
path = "benches/tier2_subsystem_read_amplification.rs"
harness = false

# =========== MOVED TO EXTENDED LANE ===========
# tier2_subsystem_compression: Adaptive compression tuning is not correctness-critical.
# Run only on explicit compression subsystem changes.
# [[bench]]
# name = "tier2_subsystem_compression"
# path = "benches/tier2_subsystem_compression.rs"
# harness = false

# =============================================================================
# Benchmarks — Tier 3: System (<30s) — ALL IN FAST LANE
# =============================================================================

[[bench]]
name = "tier3_system_compaction"
path = "benches/tier3_system_compaction.rs"
harness = false

[[bench]]
name = "tier3_system_durability"
path = "benches/tier3_system_durability.rs"
harness = false

[[bench]]
name = "tier3_system_engine"
path = "benches/tier3_system_engine.rs"
harness = false

[[bench]]
name = "tier3_system_mvcc"
path = "benches/tier3_system_mvcc.rs"
harness = false

[[bench]]
name = "tier3_system_recovery"
path = "benches/tier3_system_recovery.rs"
harness = false

[[bench]]
name = "tier3_system_scan"
path = "benches/tier3_system_scan.rs"
harness = false

[[bench]]
name = "tier3_system_sst"
path = "benches/tier3_system_sst.rs"
harness = false

# =============================================================================
# Benchmarks — Tier 4: Streaming & YCSB (>30s)
# =============================================================================

# ─────────────────────────────────────────────────────────────────────────
# TIER 4: FAST LANE (every push to main)
# Core signal tests representing canonical production workloads.
# ─────────────────────────────────────────────────────────────────────────

[[bench]]
name = "tier4_ycsb_workload_b"
path = "benches/tier4_ycsb_workload_b.rs"
harness = false

[[bench]]
name = "tier4_ycsb_workload_d"
path = "benches/tier4_ycsb_workload_d.rs"
harness = false

[[bench]]
name = "tier4_ycsb_workload_e"
path = "benches/tier4_ycsb_workload_e.rs"
harness = false

[[bench]]
name = "tier4_ycsb_workload_f"
path = "benches/tier4_ycsb_workload_f.rs"
harness = false

[[bench]]
name = "tier4_system_engine_batch_throughput"
path = "benches/tier4_system_engine_batch_throughput.rs"
harness = false

[[bench]]
name = "tier4_system_recovery_throughput"
path = "benches/tier4_system_recovery_throughput.rs"
harness = false

# ─────────────────────────────────────────────────────────────────────────
# TIER 4: EXTENDED LANE (nightly / on conditional triggers)
# Exploratory and optional workloads; run opportunistically, not per-push.
# ─────────────────────────────────────────────────────────────────────────

# tier4_ycsb_workload_a: 50/50 hybrid workload. Not canonical production.
# Moved to extended; productivity modeled via B + streaming.
# [[bench]]
# name = "tier4_ycsb_workload_a"
# path = "benches/tier4_ycsb_workload_a.rs"
# harness = false

# tier4_ycsb_workload_c: 100% read cache-only. Covered by tier3_system_scan + B.
# [[bench]]
# name = "tier4_ycsb_workload_c"
# path = "benches/tier4_ycsb_workload_c.rs"
# harness = false

# tier4_streaming_workload: Streaming insert pattern. Run on WAL/memtable changes.
# [[bench]]
# name = "tier4_streaming_workload"
# path = "benches/tier4_streaming_workload.rs"
# harness = false

# tier4_system_compaction_throughput: Extended validation of compaction at scale.
# Keep in extended; tier3_system_compaction covers correctness.
# [[bench]]
# name = "tier4_system_compaction_throughput"
# path = "benches/tier4_system_compaction_throughput.rs"
# harness = false

# tier4_system_durability_cloud (basic): Base happy-path cloud test (moved to extended).
# Extended versions with failure scenarios are new.
# [[bench]]
# name = "tier4_system_durability_cloud"
# path = "benches/tier4_system_durability_cloud.rs"
# harness = false

# =============================================================================
# Benchmarks — Tier 4: NEW HIGH-PRIORITY TESTS (Extended Lane)
# =============================================================================
# These are NEW tests addressing critical coverage gaps identified in the audit.
# They will be enabled in extended lane once they stabilize.

# tier4_durability_cloud_failure_scenarios: Cloud durability under realistic failures
# - Transient network errors with retry
# - Partial uploads + metadata failures
# - Crash during in-flight commits
# - Idempotent replay validation
[[bench]]
name = "tier4_durability_cloud_failure_scenarios"
path = "benches/tier4_durability_cloud_failure_scenarios.rs"
harness = false

# tier4_mvcc_snapshot_isolation_under_concurrency: MVCC isolation validation under load
# - Multi-threaded snapshot readers + concurrent writers
# - Compaction happening in background
# - Validates no dirty reads, no lost updates
# - Measures writer fairness under long-running snapshots
[[bench]]
name = "tier4_mvcc_snapshot_isolation_under_concurrency"
path = "benches/tier4_mvcc_snapshot_isolation_under_concurrency.rs"
harness = false
